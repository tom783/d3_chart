import {
  select,
  brushX,
  area,
  scaleLinear,
  extent,
  scaleUtc,
  axisBottom,
  axisLeft,
  curveMonotoneX,
  utcHour,
  bisector,
  pointer,
} from "d3"

import moment from "moment"

const bindingSync = () => {}

export default bindingSync
